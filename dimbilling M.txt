let
  Source = Sql.Database(ServerName, "BSHHomeHealthHospice", [Query="   -- PDGM-BP REBUILD (PDGM-first) – Includes zero-visit/request BPs#(lf)   -- Ghost Rule: BP is Ghost when TotalBillableVisits = 0#(lf)   -- LUPA Rule: requires billable visits > 0 and visits < threshold#(lf)#(lf)#(lf)WITH#(lf)Params AS (#(lf)    SELECT#(lf)        CAST(DATEADD(MONTH, -13, DATEADD(MONTH, DATEDIFF(MONTH, 0, GETDATE()), 0)) AS DATE) AS StartWindow,#(lf)        CAST(DATEADD(DAY, 60, GETDATE()) AS DATE) AS EndWindowPlus60,#(lf)        CAST(GETDATE() - 1 AS DATE) AS Yesterday#(lf)),#(lf)#(lf)rp AS (#(lf)    SELECT DISTINCT#(lf)        cea.EpiId AS EpisodeId,#(lf)        pp.PpPeriodNumber,#(lf)        pp.PpStartDate,#(lf)        pp.PpEndDate,#(lf)        rp.RpId,#(lf)        rp.RpCode,#(lf)        rp.RpSoeEffectiveFrom,#(lf)        rp.RpSoeEffectiveTo,#(lf)        cea.SourceSystem#(lf)    FROM dbo.ClientEpisodesAll AS cea#(lf)    INNER JOIN dbo.ClientEpisodeFs AS cefs#(lf)        ON cea.EpiId = cefs.CefsEpiId#(lf)       AND cea.SourceSystem = cefs.SourceSystem#(lf)    INNER JOIN dbo.PdgmPeriod AS pp#(lf)        ON cefs.CefsId = pp.PpCefsId#(lf)       AND pp.PpDeleted = 0#(lf)       AND cefs.SourceSystem = pp.SourceSystem#(lf)    INNER JOIN dbo.RatePeriod AS rp#(lf)        ON pp.PpStartDate BETWEEN rp.RpSoeEffectiveFrom AND rp.RpSoeEffectiveTo#(lf)       AND rp.RpActive = 'Y'#(lf)       AND rp.RpSltId = 1#(lf)       AND rp.RpRptId = 5#(lf)       AND cea.SourceSystem = rp.SourceSystem#(lf)    WHERE cea.EpiSlId = 1#(lf)),#(lf)#(lf)pd AS (#(lf)    SELECT#(lf)        t1.EpisodeId,#(lf)        t1.PpPeriodNumber,#(lf)        t1.PpStartDate,#(lf)        t1.PpEndDate,#(lf)        t1.RpId AS RatePeriodId,#(lf)        t1.RpCode,#(lf)        t1.RpSoeEffectiveFrom,#(lf)        t1.RpSoeEffectiveTo,#(lf)        t1.SourceSystem#(lf)    FROM rp AS t1#(lf)    INNER JOIN (#(lf)        SELECT EpisodeId, PpPeriodNumber, MIN(RpSoeEffectiveFrom) AS MinEffectiveFrom, SourceSystem#(lf)        FROM rp#(lf)        GROUP BY EpisodeId, PpPeriodNumber, SourceSystem#(lf)    ) AS t2#(lf)      ON t1.EpisodeId = t2.EpisodeId#(lf)     AND t1.PpPeriodNumber = t2.PpPeriodNumber#(lf)     AND t1.RpSoeEffectiveFrom = t2.MinEffectiveFrom#(lf)     AND t1.SourceSystem = t2.SourceSystem#(lf)),#(lf)#(lf)EBP AS (#(lf)    SELECT DISTINCT#(lf)        pp.PpId AS PPId,#(lf)        CASE WHEN pp.PpCurrentHipps <> '' THEN pp.PpCurrentHipps#(lf)             WHEN pp.PpInitialHipps <> '' THEN pp.PpInitialHipps#(lf)             ELSE NULL END AS HIPPS,#(lf)        COALESCE(phc.PhLupaThreshold, phi.PhLupaThreshold) AS LupaThresholdOld,#(lf)        COALESCE(phc.PhClinicalGrouping, phi.PhClinicalGrouping) AS ClinicalGrouping,#(lf)        COALESCE(phc.PhTiming, phi.PhTiming) AS Timing,#(lf)        COALESCE(phc.PhFunctionalImpairmentLevel, phi.PhFunctionalImpairmentLevel) AS FunctionalImpLevel,#(lf)        COALESCE(phc.PhComorbidityAdjustment, phi.PhComorbidityAdjustment) AS ComorbidityAdjustment,#(lf)        COALESCE(phc.PhCaseMixWeight, phi.PhCaseMixWeight) AS CaseMixWeight,#(lf)        cea.EpiId AS EpisodeId,#(lf)        cefs.CefsPsId AS BPInsuranceId,#(lf)        pp.PpStartDate AS BPStartDate,#(lf)        pp.PpEndDate AS BPEndDate,#(lf)        pp.PpPeriodNumber AS BPSeq,#(lf)        ca.CaId AS AdmissionId,#(lf)        cefs.CefsPsId AS InsuranceId,#(lf)        cea.SourceSystem#(lf)    FROM dbo.ClientEpisodesAll AS cea WITH (NOLOCK)#(lf)    INNER JOIN dbo.ClientEpisodeFs AS cefs WITH (NOLOCK)#(lf)        ON cea.EpiId = cefs.CefsEpiId#(lf)       AND cea.SourceSystem = cefs.SourceSystem#(lf)    INNER JOIN dbo.HCHBPdgmPeriod AS pp WITH (NOLOCK)#(lf)        ON cefs.CefsId = pp.PpCefsId#(lf)       AND cefs.SourceSystem = pp.SourceSystem#(lf)    LEFT JOIN dbo.HCHBClientAdmission AS ca WITH (NOLOCK)#(lf)        ON cea.EpiPaId = ca.CaPaId#(lf)       AND cea.EpiSlId = ca.CaSlId#(lf)       AND cea.EpiSOCDate = ca.CaSOCDate#(lf)       AND cea.SourceSystem = ca.SourceSystem#(lf)    LEFT JOIN pd AS erp#(lf)        ON cea.EpiId = erp.EpisodeId#(lf)       AND pp.PpPeriodNumber = erp.PpPeriodNumber#(lf)       AND cea.SourceSystem = erp.SourceSystem#(lf)    LEFT JOIN dbo.HCHBPdgmHipps AS phc WITH (NOLOCK)#(lf)        ON pp.PpCurrentHipps = phc.PhHipps#(lf)       AND erp.RatePeriodId = phc.PhRpId#(lf)       AND pp.SourceSystem = phc.SourceSystem#(lf)    LEFT JOIN dbo.HCHBPdgmHipps AS phi WITH (NOLOCK)#(lf)        ON pp.PpInitialHipps = phi.PhHipps#(lf)       AND erp.RatePeriodId = phi.PhRpId#(lf)       AND pp.SourceSystem = phi.SourceSystem#(lf)    WHERE pp.PpDeleted = 0#(lf)      AND cea.EpiStatus NOT IN ('DELETED','PENDING','NON-ADMIT')#(lf)      AND cea.EpiSlId = 1#(lf)),#(lf)#(lf)EpDims AS (#(lf)    SELECT DISTINCT#(lf)        cea.EpiId AS EpisodeId,#(lf)        cea.SourceSystem,#(lf)        cea.EpiStartOfEpisode,#(lf)        cea.EpiEndOfEpisode,#(lf)        cea.EpiSOCDate,#(lf)        cea.EpiStatus,#(lf)        cea.EpiBranchcode AS BranchCode,#(lf)        b.Id AS skOrgKey,#(lf)        RIGHT(b.ForeignCode, 5) AS [Location],#(lf)        p.PatientId,#(lf)        p.Address1 AS PatientAddress,#(lf)        cefs.CefsPsId AS InsuranceId,#(lf)        i.InsuranceTypeId,#(lf)        i.InsuranceType,#(lf)        cea.EpiLastName,#(lf)        cea.EpiFirstName,#(lf)        cea.EpiMI,#(lf)        CONCAT_WS(' ', CONCAT(cea.EpiLastName, ', ', cea.EpiFirstName), cea.EpiMI) AS PatientName#(lf)    FROM dbo.ClientEpisodesAll AS cea WITH (NOLOCK)#(lf)    INNER JOIN dbo.dimBranch AS b WITH (NOLOCK)#(lf)        ON cea.EpiBranchcode = b.BranchCode#(lf)       AND cea.SourceSystem = b.SourceSystem#(lf)    INNER JOIN dbo.dimPatient AS p WITH (NOLOCK)#(lf)        ON cea.EpiPaId = p.PatientId#(lf)       AND cea.SourceSystem = p.SourceSystem#(lf)    LEFT JOIN dbo.ClientEpisodeFs AS cefs WITH (NOLOCK)#(lf)        ON cea.EpiId = cefs.CefsEpiId#(lf)       AND cefs.CefsPs = 'P'#(lf)       AND cefs.CefsActive = 'Y'#(lf)       AND cea.SourceSystem = cefs.SourceSystem#(lf)    LEFT JOIN dbo.dimInsurance AS i WITH (NOLOCK)#(lf)        ON cefs.CefsPsId = i.InsuranceId#(lf)       AND cefs.SourceSystem = i.SourceSystem#(lf)    WHERE cea.EpiSlId = 1#(lf)      AND cea.EpiStatus NOT IN ('DELETED','PENDING','NON-ADMIT')#(lf)),#(lf)#(lf)BP_Requests AS (#(lf)    SELECT#(lf)        r.ReqId,#(lf)        r.EpiId,#(lf)        de.StartOfEpisodeDate,#(lf)        de.EndOfEpisodeDate,#(lf)        r.ServiceLineId,#(lf)        r.ShiftDate,#(lf)        FORMAT(r.ShiftDate, 'yyyyMMdd') AS ShiftDateKey,#(lf)        r.ShiftHrs,#(lf)        jd.JdCode,#(lf)        jd.JdDescription AS Discipline,#(lf)        r.RateType,#(lf)        ISNULL(r.FlatType, '*') AS ServiceCode,#(lf)        CASE WHEN sc.ScBillable = 'Y' THEN 1 ELSE 0 END AS ServiceBillable,#(lf)        ef.BPSeq,#(lf)        CASE WHEN ef.BPSeq IS NOT NULL#(lf)             THEN CONVERT(VARCHAR(20), r.EpiId) + '-' + CONVERT(VARCHAR(10), ef.BPSeq)#(lf)             ELSE NULL END AS BPKey,#(lf)        r.NumberRequested AS Requested,#(lf)        r.OrderID AS RequestOrderId,#(lf)        r.Scheduled,#(lf)        r.NoNeedNum,#(lf)        r.TransferStatus#(lf)    FROM dbo.Request AS r#(lf)    INNER JOIN dbo.JobDescriptions AS jd#(lf)        ON r.JobDId = jd.JdId#(lf)    LEFT JOIN dbo.ServiceCodes AS sc#(lf)        ON sc.ScCode = r.FlatType#(lf)    LEFT JOIN dbo.dimEpisode AS de#(lf)        ON r.EpiId = de.EpisodeId#(lf)       AND r.SourceSystem = de.SourceSystem#(lf)    CROSS APPLY (#(lf)        SELECT CASE#(lf)            WHEN DATEDIFF(DAY, de.StartOfEpisodeDate, r.ShiftDate) <= 30 THEN 1#(lf)            WHEN DATEDIFF(DAY, de.StartOfEpisodeDate, r.ShiftDate) >= 31 THEN 2#(lf)            ELSE NULL#(lf)        END AS BPSeq#(lf)    ) AS ef#(lf)    WHERE NOT (r.NumberRequested > (r.Scheduled + r.NoNeedNum) AND r.TransferStatus = 3)#(lf)      AND r.ServiceLineId = 1#(lf)      AND r.NoNeedNum = 0#(lf)      AND r.TransferStatus <> 3#(lf)      AND sc.ScBillable = 'Y'#(lf)      AND r.Scheduled = 0#(lf)),#(lf)BP_Requests_Summary AS (#(lf)    SELECT#(lf)        BPKey,#(lf)        BPSeq AS PpPeriodNumber,#(lf)        SUM(Requested) AS Requests#(lf)    FROM (#(lf)        SELECT DISTINCT BPKey, BPSeq, ReqId, Requested, ServiceBillable#(lf)        FROM BP_Requests#(lf)        WHERE BPKey IS NOT NULL#(lf)    ) AS dedup#(lf)    GROUP BY BPKey, BPSeq#(lf)),#(lf)#(lf)TaskDetails AS (#(lf)    SELECT#(lf)        csv.CsvId AS ScheduledVisitId,#(lf)        cev.CevId AS VisitId,#(lf)        cea.EpiId AS EpisodeId,#(lf)        csv.CsvLastUpdate AS LastUpdateDate,#(lf)        MAX(csv.CsvLastUpdate) OVER (#(lf)            PARTITION BY csv.CsvEpiId, csv.CsvSchedDate, csv.CsvId, sc.ScCode#(lf)        ) AS MaxUpdateDate,#(lf)        CASE WHEN EBP.BPSeq IS NOT NULL THEN CAST(cea.EpiId AS VARCHAR(20)) + '-' + CAST(EBP.BPSeq AS VARCHAR(20)) END AS BPKey,#(lf)        EBP.BPSeq AS BillingPeriodSequence,#(lf)        CONVERT(CHAR(8), EBP.BPStartDate, 112) AS BillingPeriodStartDate,#(lf)        CONVERT(CHAR(8), EBP.BPEndDate, 112) AS BillingPeriodEndDate,#(lf)        CONVERT(CHAR(8), CEA.EpiStartOfEpisode, 112) AS EpisodeStartDate,#(lf)        CONVERT(CHAR(8), CEA.EpiEndOfEpisode, 112) AS EpisodeEndDate,#(lf)        CONVERT(CHAR(8), CEA.EpiSocDate, 112) AS StartOfCareDate,#(lf)        1 AS TaskCount,#(lf)        CASE WHEN sc.ScBillable = 'Y' THEN#(lf)            CASE#(lf)                WHEN MAX(CASE WHEN vs.VsDescription = 'COMPLETED' THEN 1 ELSE 0 END)#(lf)                     OVER (PARTITION BY csv.CsvEpiId, csv.CsvVisitNumber, csv.CsvScId, sc.ScCode) = 1#(lf)                 AND vs.VsDescription = 'COMPLETED'#(lf)                THEN CASE#(lf)                        WHEN sc.ScCode LIKE '%PRN%'#(lf)                             THEN CASE WHEN vs.VsDescription IN ('COMPLETED','MISSED') THEN 1 ELSE 0 END#(lf)                        ELSE 1#(lf)                     END#(lf)                WHEN MAX(CASE WHEN vs.VsDescription = 'COMPLETED' THEN 1 ELSE 0 END)#(lf)                     OVER (PARTITION BY csv.CsvEpiId, csv.CsvVisitNumber, csv.CsvScId, sc.ScCode) = 0#(lf)                 AND ROW_NUMBER() OVER (#(lf)                        PARTITION BY csv.CsvEpiId, csv.CsvVisitNumber, csv.CsvScId, sc.ScCode#(lf)                        ORDER BY csv.CsvLastUpdate DESC, csv.CsvSchedDate DESC#(lf)                     ) = 1#(lf)                 AND (#(lf)                        sc.ScCode NOT LIKE '%PRN%'#(lf)                        OR (sc.ScCode LIKE '%PRN%' AND vs.VsDescription IN ('COMPLETED','MISSED'))#(lf)                     )#(lf)                THEN CASE#(lf)                        WHEN sc.ScCode LIKE '%PRN%'#(lf)                             THEN CASE WHEN vs.VsDescription IN ('COMPLETED','MISSED') THEN 1 ELSE 0 END#(lf)                        ELSE 1#(lf)                     END#(lf)                ELSE 0#(lf)            END#(lf)        ELSE 0 END AS InHomeVisits_VisitNumber,#(lf)        CASE#(lf)            WHEN sc.ScBillable = 'Y'#(lf)             AND vs.VsDescription NOT IN ('COMPLETED','MISSED','DECLINED')#(lf)             AND sc.ScCode NOT LIKE '%PRN%'#(lf)             AND ROW_NUMBER() OVER (#(lf)                    PARTITION BY csv.CsvEpiId, csv.CsvVisitNumber, csv.CsvScId, sc.ScCode#(lf)                    ORDER BY csv.CsvLastUpdate DESC, csv.CsvSchedDate DESC#(lf)                 ) = 1#(lf)            THEN 1 ELSE 0#(lf)        END AS ScheduledVisits,#(lf)        CASE#(lf)            WHEN vs.VsDescription = 'COMPLETED' THEN 1#(lf)            WHEN csv.CsvSchedDate BETWEEN cea.EpiStartOfEpisode AND cea.EpiEndOfEpisode THEN 1#(lf)            ELSE 0#(lf)        END AS VisitBetweenSOEandEOE,#(lf)        CASE#(lf)            WHEN vs.VsDescription = 'COMPLETED'#(lf)             AND sc.ScCode NOT LIKE '%66%'#(lf)             AND cev.CevBillable = 1#(lf)            THEN 1 ELSE 0#(lf)        END AS CompletedVisits,#(lf)        CASE WHEN cev.CevBillable = 1 THEN 1 ELSE 0 END AS VisitBillable,#(lf)        CASE WHEN sc.ScBillable = 'Y' THEN 1 ELSE 0 END AS ServiceBillable,#(lf)        CASE WHEN sc.ScPayable = 'Y' THEN 1 ELSE 0 END AS Payable,#(lf)        CASE WHEN EBP.BPEndDate >= (SELECT Yesterday FROM Params) THEN 'Open' ELSE 'Closed' END AS BP_OpenClosedFlag,#(lf)        sc.ScDesc AS TaskDesc,#(lf)        sc.ScVisitType AS TaskType,#(lf)        sc.ScCode AS TaskCode,#(lf)        sc.ScJdCode AS TaskJobCode,#(lf)        d.DscDesc AS Discipline,#(lf)        sc.ScProductivityPoints AS ProductivityPts,#(lf)        vs.VsDescription AS TaskStatus,#(lf)        vmr.VmDesc AS VisitMissedReason,#(lf)        p.PatientId,#(lf)        p.Address1 AS PatientAddress,#(lf)        cefs.CefsPsid AS InsuranceId,#(lf)        i.InsuranceTypeId,#(lf)        i.InsuranceType,#(lf)        cea.EpiBranchcode AS BranchCode,#(lf)        b.Id AS skOrgKey,#(lf)        RIGHT(b.ForeignCode, 5) AS [Location],#(lf)        cea.EpiSlId,#(lf)        EBP.HIPPS,#(lf)        CASE WHEN COALESCE(i.InsuranceTypeId, cefs.CefsPsid) = 25024 THEN 4 ELSE EBP.LupaThresholdOld END AS LupaThreshold,#(lf)        EBP.ClinicalGrouping,#(lf)        EBP.Timing,#(lf)        EBP.FunctionalImpLevel,#(lf)        EBP.ComorbidityAdjustment,#(lf)        EBP.CaseMixWeight,#(lf)        csv.SourceSystem,#(lf)        cev.CevDeleted,#(lf)        cea.EpiStatus,#(lf)        csv.CsvVisitNumber,#(lf)        cea.EpiLastName,#(lf)        cea.EpiFirstName,#(lf)        cea.EpiMI,#(lf)        CONCAT_WS(' ', CONCAT(cea.EpiLastName, ', ', cea.EpiFirstName), cea.EpiMI) AS PatientName#(lf)    FROM dbo.ClientSchedVisits AS csv WITH (NOLOCK)#(lf)    INNER JOIN dbo.VisitStatuses AS vs WITH (NOLOCK)#(lf)        ON csv.CsvStatus = vs.VsStatus AND csv.SourceSystem = vs.SourceSystem#(lf)    INNER JOIN dbo.ServiceCodes AS sc WITH (NOLOCK)#(lf)        ON csv.CsvScid = sc.Scid AND csv.SourceSystem = sc.SourceSystem#(lf)    INNER JOIN dbo.Disciplines AS d WITH (NOLOCK)#(lf)        ON sc.ScDiscipline = d.DscCode AND sc.SourceSystem = d.SourceSystem#(lf)    INNER JOIN dbo.ClientEpisodesAll AS cea WITH (NOLOCK)#(lf)        ON csv.CsvEpiId = cea.EpiId AND csv.SourceSystem = cea.SourceSystem#(lf)    INNER JOIN dbo.dimBranch AS b WITH (NOLOCK)#(lf)        ON cea.EpiBranchcode = b.BranchCode AND cea.SourceSystem = b.SourceSystem#(lf)    INNER JOIN dbo.dimPatient AS p WITH (NOLOCK)#(lf)        ON cea.EpiPaId = p.PatientId AND cea.SourceSystem = p.SourceSystem#(lf)    LEFT JOIN EBP#(lf)        ON cea.EpiId = EBP.EpisodeId#(lf)       AND csv.CsvSchedDate BETWEEN EBP.BPStartDate AND EBP.BPEndDate#(lf)       AND cea.SourceSystem = EBP.SourceSystem#(lf)    LEFT JOIN dbo.ClientEpisodeVisitsAll AS cev WITH (NOLOCK)#(lf)        ON csv.CsvId = cev.CevCsvId AND csv.SourceSystem = cev.SourceSystem AND cev.CevDeleted = 0#(lf)    LEFT JOIN dbo.ClientEpisodeFs AS cefs WITH (NOLOCK)#(lf)        ON cea.EpiId = cefs.CefsEpiId AND cefs.CefsPs = 'P' AND cefs.CefsActive = 'Y' AND cea.SourceSystem = cefs.SourceSystem#(lf)    LEFT JOIN dbo.VisitMissedReasons AS vmr WITH (NOLOCK)#(lf)        ON csv.CsvVmId = vmr.VmId AND csv.SourceSystem = vmr.SourceSystem#(lf)    LEFT JOIN dbo.dimInsurance AS i WITH (NOLOCK)#(lf)        ON cefs.CefsPsid = i.InsuranceId AND cefs.SourceSystem = i.SourceSystem#(lf)    WHERE csv.CsvSchedDate >= (SELECT StartWindow FROM Params)#(lf)      AND csv.CsvSchedDate <= (SELECT EndWindowPlus60 FROM Params)#(lf)      AND cea.EpiSlId = 1#(lf)      AND cea.EpiStatus NOT IN ('DELETED','PENDING','NON-ADMIT')#(lf)      AND (cev.CevDeleted <> 1 OR cev.CevDeleted IS NULL)#(lf)),#(lf)#(lf)TD_Agg AS (#(lf)    SELECT#(lf)        td.EpisodeId,#(lf)        td.BillingPeriodSequence AS BPSeq,#(lf)        td.BillingPeriodStartDate AS skBillingPeriodStartDateKey,#(lf)        td.BillingPeriodEndDate   AS skBillingPeriodEndDateKey,#(lf)        td.EpisodeStartDate       AS skEpisodeStartDateKey,#(lf)        td.EpisodeEndDate         AS skEpisodeEndDateKey,#(lf)        td.StartOfCareDate        AS skStartOfCareKey,#(lf)        MAX(td.HIPPS) AS HIPPS,#(lf)        MAX(td.LupaThreshold) AS LupaThreshold,#(lf)        MAX(td.ClinicalGrouping) AS ClinicalGrouping,#(lf)        MAX(td.Timing) AS Timing,#(lf)        MAX(td.FunctionalImpLevel) AS FunctionalImpLevel,#(lf)        MAX(td.ComorbidityAdjustment) AS ComorbidityAdjustment,#(lf)        MAX(td.CaseMixWeight) AS CaseMixWeight,#(lf)        MAX(td.BP_OpenClosedFlag) AS BP_OpenClosedFlag,#(lf)        MAX(td.EpiStatus) AS EpiStatus,#(lf)        SUM(COALESCE(td.ScheduledVisits,0)) AS ScheduledVisits,#(lf)        SUM(CASE WHEN td.TaskCode LIKE '%66%' THEN 0#(lf)                 WHEN td.TaskStatus = 'MISSED' AND td.ServiceBillable = 1 THEN 1#(lf)                 ELSE 0 END) AS MissedVisits,#(lf)        SUM(CASE WHEN td.VisitBillable = 1 THEN 1 ELSE 0 END) AS TotalBillableVisits,#(lf)        SUM(CASE WHEN td.Discipline = 'PHYSICAL THERAPIST' AND td.BP_OpenClosedFlag='Closed' THEN td.CompletedVisits ELSE 0 END) AS PT,#(lf)        SUM(CASE WHEN td.Discipline = 'OCCUPATIONAL THERAPIST' AND td.BP_OpenClosedFlag='Closed' THEN td.CompletedVisits ELSE 0 END) AS OT,#(lf)        SUM(CASE WHEN td.Discipline = 'SKILLED NURSE' AND td.BP_OpenClosedFlag='Closed' THEN td.CompletedVisits ELSE 0 END) AS SN,#(lf)        SUM(CASE WHEN td.Discipline = 'SPEECH THERAPIST' AND td.BP_OpenClosedFlag='Closed' THEN td.CompletedVisits ELSE 0 END) AS ST,#(lf)        SUM(CASE WHEN td.Discipline = 'MEDICAL SOCIAL WORKER' AND td.BP_OpenClosedFlag='Closed' THEN td.CompletedVisits ELSE 0 END) AS MSW,#(lf)        SUM(CASE WHEN td.Discipline = 'HOME HEALTH AIDE' AND td.BP_OpenClosedFlag='Closed' THEN td.CompletedVisits ELSE 0 END) AS HHA,#(lf)        SUM(CASE WHEN td.BP_OpenClosedFlag='Closed' THEN td.CompletedVisits ELSE 0 END) AS TotalCompleted_Closed,#(lf)        SUM(CASE WHEN td.Discipline = 'PHYSICAL THERAPIST' AND td.BP_OpenClosedFlag='Open' THEN td.InHomeVisits_VisitNumber ELSE 0 END) AS PT_Open,#(lf)        SUM(CASE WHEN td.Discipline = 'OCCUPATIONAL THERAPIST' AND td.BP_OpenClosedFlag='Open' THEN td.InHomeVisits_VisitNumber ELSE 0 END) AS OT_Open,#(lf)        SUM(CASE WHEN td.Discipline = 'SKILLED NURSE' AND td.BP_OpenClosedFlag='Open' THEN td.InHomeVisits_VisitNumber ELSE 0 END) AS SN_Open,#(lf)        SUM(CASE WHEN td.Discipline = 'SPEECH THERAPIST' AND td.BP_OpenClosedFlag='Open' THEN td.InHomeVisits_VisitNumber ELSE 0 END) AS ST_Open,#(lf)        SUM(CASE WHEN td.Discipline = 'MEDICAL SOCIAL WORKER' AND td.BP_OpenClosedFlag='Open' THEN td.InHomeVisits_VisitNumber ELSE 0 END) AS MSW_Open,#(lf)        SUM(CASE WHEN td.Discipline = 'HOME HEALTH AIDE' AND td.BP_OpenClosedFlag='Open' THEN td.InHomeVisits_VisitNumber ELSE 0 END) AS HHA_Open,#(lf)        SUM(CASE WHEN td.BP_OpenClosedFlag='Open' THEN td.InHomeVisits_VisitNumber ELSE 0 END) AS TotalOpen,#(lf)        SUM(CASE WHEN td.BP_OpenClosedFlag='Open' THEN td.CompletedVisits ELSE 0 END) AS TotalCompleted_Open,#(lf)        SUM(td.ServiceBillable) AS AnyBillableService,#(lf)        SUM(CASE WHEN td.CompletedVisits = 1 THEN 1 ELSE 0 END) AS CompletedBillableCount#(lf)    FROM TaskDetails td#(lf)    GROUP BY#(lf)        td.EpisodeId,#(lf)        td.BillingPeriodSequence,#(lf)        td.BillingPeriodStartDate,#(lf)        td.BillingPeriodEndDate,#(lf)        td.EpisodeStartDate,#(lf)        td.EpisodeEndDate,#(lf)        td.StartOfCareDate#(lf)),#(lf)#(lf)EBP_Window AS (#(lf)    SELECT#(lf)        e.*,#(lf)        CAST(e.EpisodeId AS VARCHAR(20)) + '-' + CAST(e.BPSeq AS VARCHAR(20)) AS BPKey#(lf)    FROM EBP e#(lf)    CROSS JOIN Params p#(lf)    WHERE e.BPStartDate >= p.StartWindow#(lf)      AND e.BPStartDate <= p.EndWindowPlus60#(lf)),#(lf)#(lf)/* ---------- UPDATED GHOST LOGIC APPLIED HERE ---------- */#(lf)FB AS (#(lf)    SELECT#(lf)        e.EpisodeId,#(lf)        e.BPKey,#(lf)        CONVERT(CHAR(8), e.BPStartDate, 112) AS skBillingPeriodStartDateKey,#(lf)        CONVERT(CHAR(8), e.BPEndDate,   112) AS skBillingPeriodEndDateKey,#(lf)        CONVERT(CHAR(8), d.EpiStartOfEpisode, 112) AS skEpisodeStartDateKey,#(lf)        CONVERT(CHAR(8), d.EpiEndOfEpisode,   112) AS skEpisodeEndDateKey,#(lf)        CONVERT(CHAR(8), d.EpiSOCDate,        112) AS skStartOfCareKey,#(lf)        e.BPSeq AS BillingPeriodSequence,#(lf)#(lf)        d.PatientId,#(lf)        CAST(d.PatientId AS VARCHAR(50)) + '_' + d.SourceSystem AS PatientSystemId,#(lf)        d.PatientName,#(lf)        d.PatientAddress,#(lf)        d.InsuranceId,#(lf)        d.InsuranceTypeId,#(lf)        d.InsuranceType,#(lf)        d.BranchCode,#(lf)        d.[Location],#(lf)#(lf)        COALESCE(t.HIPPS, e.HIPPS) AS HIPPS,#(lf)        CASE WHEN COALESCE(d.InsuranceTypeId, e.InsuranceId) = 25024 THEN 4#(lf)             ELSE COALESCE(t.LupaThreshold, e.LupaThresholdOld)#(lf)        END AS LupaThreshold,#(lf)        COALESCE(t.ClinicalGrouping, e.ClinicalGrouping) AS ClinicalGrouping,#(lf)        COALESCE(t.Timing, e.Timing) AS Timing,#(lf)        COALESCE(t.FunctionalImpLevel, e.FunctionalImpLevel) AS FunctionalImpLevel,#(lf)        COALESCE(t.ComorbidityAdjustment, e.ComorbidityAdjustment) AS ComorbidityAdjustment,#(lf)        COALESCE(t.CaseMixWeight, e.CaseMixWeight) AS CaseMixWeight,#(lf)#(lf)        CASE WHEN e.BPEndDate >= (SELECT Yesterday FROM Params) THEN 'Open' ELSE 'Closed' END AS BP_OpenClosedFlag,#(lf)#(lf)        d.EpiStatus,#(lf)#(lf)        COALESCE(t.ScheduledVisits, 0)        AS ScheduledVisits,#(lf)        COALESCE(t.MissedVisits, 0)           AS MissedVisits,#(lf)        COALESCE(t.TotalBillableVisits, 0)    AS TotalBillableVisits,#(lf)#(lf)        COALESCE(t.PT, 0)  AS PT,#(lf)        COALESCE(t.OT, 0)  AS OT,#(lf)        COALESCE(t.SN, 0)  AS SN,#(lf)        COALESCE(t.ST, 0)  AS ST,#(lf)        COALESCE(t.MSW, 0) AS MSW,#(lf)        COALESCE(t.HHA, 0) AS HHA,#(lf)        COALESCE(t.TotalCompleted_Closed, 0) AS TotalCompleted_Closed,#(lf)#(lf)        COALESCE(t.PT_Open, 0) AS PT_Open,#(lf)        COALESCE(t.OT_Open, 0) AS OT_Open,#(lf)        COALESCE(t.SN_Open, 0) AS SN_Open,#(lf)        COALESCE(t.ST_Open, 0) AS ST_Open,#(lf)        COALESCE(t.MSW_Open, 0) AS MSW_Open,#(lf)        COALESCE(t.HHA_Open, 0) AS HHA_Open,#(lf)        COALESCE(t.TotalOpen, 0) AS TotalOpen,#(lf)        COALESCE(t.TotalCompleted_Open, 0) AS TotalCompleted_Open,#(lf)#(lf)        CASE WHEN COALESCE(t.AnyBillableService, 0) > 0 THEN 'Y' ELSE 'N' END AS HasBillableService,#(lf)#(lf)        /* --- UPDATED: Ghost is strictly ""0 billable visits in the BP"" --- */#(lf)        CASE WHEN e.BPEndDate >= (SELECT Yesterday FROM Params)#(lf)               AND COALESCE(t.TotalBillableVisits, 0) = 0#(lf)             THEN 1 ELSE 0 END AS GhostBillingPeriodOpen,#(lf)#(lf)        CASE WHEN e.BPEndDate < (SELECT Yesterday FROM Params)#(lf)               AND COALESCE(t.TotalBillableVisits, 0) = 0#(lf)             THEN 1 ELSE 0 END AS GhostBillingPeriodClosed#(lf)    FROM EBP_Window e#(lf)    LEFT JOIN TD_Agg t#(lf)        ON e.EpisodeId = t.EpisodeId#(lf)       AND e.BPSeq     = t.BPSeq#(lf)    LEFT JOIN EpDims d#(lf)        ON e.EpisodeId = d.EpisodeId#(lf)       AND e.SourceSystem = d.SourceSystem#(lf)),#(lf)#(lf)/* ---------- UPDATED LUPA LOGIC (requires billable>0) ---------- */#(lf)FB_LUPA AS (#(lf)    SELECT#(lf)        fb.*,#(lf)        CASE#(lf)            WHEN fb.BP_OpenClosedFlag = 'Closed'#(lf)             AND fb.TotalBillableVisits > 0#(lf)             AND fb.TotalCompleted_Closed < fb.LupaThreshold#(lf)            THEN 1 ELSE 0#(lf)        END AS IsLupaBP,#(lf)        CASE#(lf)            WHEN fb.BP_OpenClosedFlag = 'Open'#(lf)             AND fb.TotalBillableVisits > 0#(lf)             AND fb.TotalOpen > 0#(lf)             AND fb.TotalOpen < fb.LupaThreshold#(lf)            THEN 1 ELSE 0#(lf)        END AS IsLupaBP_Open#(lf)    FROM FB fb#(lf))#(lf)#(lf)SELECT#(lf)    f.EpisodeId,#(lf)    f.BPKey,#(lf)    f.skBillingPeriodStartDateKey,#(lf)    f.skBillingPeriodEndDateKey,#(lf)    f.skEpisodeStartDateKey,#(lf)    f.skEpisodeEndDateKey,#(lf)    f.skStartOfCareKey,#(lf)    f.BillingPeriodSequence,#(lf)    f.PatientId,#(lf)    f.PatientName,#(lf)    f.PatientSystemId,#(lf)    f.PatientAddress,#(lf)    f.InsuranceId,#(lf)    f.InsuranceTypeId,#(lf)    f.InsuranceType,#(lf)    f.BranchCode,#(lf)    f.[Location],#(lf)    f.HIPPS,#(lf)    f.LupaThreshold,#(lf)    f.ClinicalGrouping,#(lf)    f.Timing,#(lf)    f.FunctionalImpLevel,#(lf)    f.ComorbidityAdjustment,#(lf)    f.CaseMixWeight,#(lf)    f.BP_OpenClosedFlag,#(lf)    f.EpiStatus,#(lf)    COALESCE(brs.Requests, 0) AS Requests,#(lf)    f.MissedVisits,#(lf)    f.ScheduledVisits,#(lf)    f.HasBillableService,#(lf)    f.TotalBillableVisits,#(lf)    FORMAT(CONVERT(DATE, f.skBillingPeriodStartDateKey, 112), 'M/dd/yyyy') AS BillingPeriodStartDate,#(lf)    FORMAT(CONVERT(DATE, f.skBillingPeriodEndDateKey, 112), 'M/dd/yyyy')   AS BillingPeriodEndDate,#(lf)    FORMAT(CONVERT(DATE, f.skEpisodeStartDateKey, 112), 'M/dd/yyyy')       AS EpisodeStartDate,#(lf)    FORMAT(CONVERT(DATE, f.skEpisodeEndDateKey, 112), 'M/dd/yyyy')         AS EpisodeEndDate,#(lf)    FORMAT(CONVERT(DATE, f.skStartOfCareKey, 112), 'M/dd/yyyy')            AS SOCDate,#(lf)    f.BranchCode + '000' + CAST(f.PatientId AS VARCHAR(20)) + '0' + CAST(1 AS VARCHAR(1)) AS MRN,#(lf)    f.PT, f.OT, f.SN, f.ST, f.MSW, f.HHA,#(lf)    f.TotalCompleted_Closed,#(lf)    f.IsLupaBP,#(lf)    f.PT_Open, f.OT_Open, f.SN_Open, f.ST_Open, f.MSW_Open, f.HHA_Open,#(lf)    f.TotalOpen,#(lf)    f.TotalCompleted_Open,#(lf)    f.IsLupaBP_Open,#(lf)    f.GhostBillingPeriodOpen,#(lf)    f.GhostBillingPeriodClosed#(lf)FROM FB_LUPA AS f#(lf)LEFT JOIN BP_Requests_Summary AS brs#(lf)    ON f.BPKey = brs.BPKey#(lf)ORDER BY f.skBillingPeriodStartDateKey DESC, f.EpisodeId ASC, f.BPKey ASC;#(lf)", CommandTimeout=#duration(0, 0, 10, 0)]),
    #"Renamed Columns" = Table.RenameColumns(Source,{{"BillingPeriodEndDate", "Billing Period End Date"}, {"BillingPeriodStartDate", "Billing Period Start Date"}, {"LupaThreshold", "LUPA Threshold"}, {"EpisodeId", "Episode ID"}}),
    #"Changed Type" = Table.TransformColumnTypes(#"Renamed Columns",{{"Billing Period Start Date", type date}, {"Billing Period End Date", type date}, {"EpisodeStartDate", type date}, {"EpisodeEndDate", type date}, {"SOCDate", type date},{"LUPA Threshold", Int64.Type}}),
    #"Ghost Flag" = Table.ReplaceValue(
    Table.ReplaceValue(
        Table.TransformColumnTypes(
            Table.DuplicateColumn(#"Changed Type", "GhostBillingPeriodOpen", "Ghost Flag"),
            {{"Ghost Flag", type text}}
        ),
        "0", "No", Replacer.ReplaceText, {"Ghost Flag"}
    ),
    "1", "Yes", Replacer.ReplaceText, {"Ghost Flag"}
),
    #"Renamed Columns2" = Table.RenameColumns(#"Ghost Flag",{{"TotalOpen", "TotalOpen (Missed Not Included)"}}),
    #"Planned Visits" = Table.AddColumn(#"Renamed Columns2", "Total Planned Visits", each [TotalCompleted_Open]+[Requests]+[ScheduledVisits]),
    #"Visits Needed-Potential" = Table.AddColumn(#"Planned Visits", "Visits Needed (Potential)", each [LUPA Threshold] - [Total Planned Visits]),
    #"Visits Needed-Actual" = Table.AddColumn(#"Visits Needed-Potential", "Visits Needed (Actual)", each [LUPA Threshold] - [TotalCompleted_Open]),
    IsActual = Table.AddColumn(#"Visits Needed-Actual", "IsActualLUPA", each if [LUPA Threshold] = null then "No" else if [TotalCompleted_Open] < [LUPA Threshold] then "Yes" else "No"),
    IsPotential = Table.AddColumn(IsActual, "IsPotentialLUPA", each if [LUPA Threshold] = null then "No" else if [Total Planned Visits] < [LUPA Threshold] then "Yes" else "No"),
    #"Changed Type2" = Table.TransformColumnTypes(IsPotential,{{"Visits Needed (Potential)", Int64.Type}, {"Total Planned Visits", Int64.Type}})
in
    #"Changed Type2"